import useGetTokenSuffix from "@/hooks/useGetTokenSuffix";

export default function FetchGlobal() {
  useGetTokenSuffix();
  // const setPriceSol = useSetPriceSol();
  // const { price } = useGetPrice({});
  // useEffect(() => {
  //   setPriceSol(price);
  // }, [price]);
  return null;
}
